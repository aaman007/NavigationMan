import React from 'react';
import Center from "../utils/Center";
import { Text } from "react-native";

const Feed = props => {
    return (
        <Center>
            <Text> News Feed </Text>
        </Center>
    )
};

export default Feed;
