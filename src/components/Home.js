import React from 'react';

import { View, Text, StyleSheet } from 'react-native';
import Center from "../utils/Center";

const Home = props => {
    return (
        <Center>
            <Text> Home Tab </Text>
        </Center>
    )
};

export default Home;
